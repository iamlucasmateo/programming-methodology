package ProgrammingAssignment5;

public class DiceSelectionResponse {
    DiceSelectionResponse(String response) {
        // if response in Enum of strategy types
        // this.strategy = not null;
        // this.dice = {0, 0, 0, 0, 0}
        // else
        // this.strategy = null;
        // this.dice = processedDices, and pass it to function        
    }
}
